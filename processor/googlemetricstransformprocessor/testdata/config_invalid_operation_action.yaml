receivers:
    nop:

processors:
    googlemetricstransform:
        transforms:
          - include: old_name
            action: update
            operations:
                - action: invalid

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]