# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for varnish metrics.
    type: object
    properties:
      varnish.backend.connection.count:
        description: "VarnishBackendConnectionCountMetricConfig provides config for the varnish.backend.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.backend.request.count:
        description: "VarnishBackendRequestCountMetricConfig provides config for the varnish.backend.request.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.cache.operation.count:
        description: "VarnishCacheOperationCountMetricConfig provides config for the varnish.cache.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.client.request.count:
        description: "VarnishClientRequestCountMetricConfig provides config for the varnish.client.request.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.client.request.error.count:
        description: "VarnishClientRequestErrorCountMetricConfig provides config for the varnish.client.request.error.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.object.count:
        description: "VarnishObjectCountMetricConfig provides config for the varnish.object.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.object.expired:
        description: "VarnishObjectExpiredMetricConfig provides config for the varnish.object.expired metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.object.moved:
        description: "VarnishObjectMovedMetricConfig provides config for the varnish.object.moved metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.object.nuked:
        description: "VarnishObjectNukedMetricConfig provides config for the varnish.object.nuked metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.session.count:
        description: "VarnishSessionCountMetricConfig provides config for the varnish.session.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      varnish.thread.operation.count:
        description: "VarnishThreadOperationCountMetricConfig provides config for the varnish.thread.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for varnish resource attributes.
    type: object
    properties:
      varnish.cache.name:
        description: ResourceAttributeConfig provides common config for a varnish.cache.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for varnish metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
