receivers:
    nop:

processors:
    googlemetricstransform:
        transforms:
            - include: old_name
              action: insert # missing new_name

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]