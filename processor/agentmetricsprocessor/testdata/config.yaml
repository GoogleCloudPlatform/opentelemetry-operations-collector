receivers:
  examplereceiver:

processors:
  agentmetrics:

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [agentmetrics]
      exporters: [exampleexporter]
