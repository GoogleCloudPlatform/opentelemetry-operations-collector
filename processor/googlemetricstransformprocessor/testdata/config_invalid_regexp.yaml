receivers:
    nop:

processors:
    googlemetricstransform:
        transforms:
            - include: old[\da
              match_type: regexp
              action: update

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]