receivers:
    nop:

processors:
    googlemetricstransform:
        transforms:
          - include: old_name
            action: invalid # invalid action type

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [googlemetricstransform]
            exporters: [nop]