import 'common.gcl' as common

config build = common.build {
  build_file = 'otelcol-google/kokoro/scripts/build/package_macos.sh'

  params {
    artifacts = common.pkg_artifacts_patterns + ['**/*.dmg']
  }
}
